{
  "name": "pokedex",
  "version": "0.1.0",
  "description": "Pokedex challenge",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --scope @pokedex/web --scope @pokedex/server --parallel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MoonTory/pokedex.git"
  },
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
